[tools]
# modmux is installed via GitHub Releases binary or deno

[settings]
# See: https://github.com/myty/modmux/releases

[tasks.install]
description = "Install modmux globally via deno"
run = "deno install --global --allow-all -n modmux --config deno.json --force cli/src/main.ts"
